#include "holberton.h"
/**
*_abs - prints the sign of a number.
*@n: input character
* Return: 0 if is zero , 1 if is positive and -1 if is negative.
*/
int _abs(int n)
{

	if (n < 0)
	{
	n = n * -1;
	}
	else
	{
	}
	return (n);
}
